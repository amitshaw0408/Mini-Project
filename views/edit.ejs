<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="p-4">

    <h1 class="text-3xl font-bold "><i>Hello!  </i><%= user.name %> 😎</h1>
    <p class="text-sm">Welcome to your Edit page.</p>
    <p class="text-sm text-zinc-400 mt-6">Edit your posts 👇</p>

    <form action="/update/<%= post._id %>" class="flex items-end mt4" method="post">
        <textarea name="postContent" class="mt-3 border-2 rounded-lg border-gray-300 p-3 h-40 w-1/3 resize-none font-serif" placeholder="What's you want to edit?"><%= post.content %></textarea>
        <input type="submit" value="Submit" class="ml-4 bg-blue-500 text-white px-10 py-2 rounded-lg hover:bg-blue-600 focus:bg-blue-300">
    </form>

</body>
</html>